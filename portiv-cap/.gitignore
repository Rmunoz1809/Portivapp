node_modules/
.DS_Store
# www/portiv-iap.js SÍ se versiona: es el bundle que carga el build de iOS y sin él
# un clone limpio no arranca. (Su fuente, src/iap.js, y package.json siguen SIN
# rastrear en el repo — hay que añadirlos para que `npm run build:iap` sea posible.)
/ios/App/Pods/
/ios/App/App/public/
/ios/DerivedData/
