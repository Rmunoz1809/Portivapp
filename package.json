{
  "name": "portiv-build",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Paso de build de Portiv: genera dist/index.html minificado. El fuente (index.html) queda intacto con todos sus comentarios.",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "html-minifier-terser": "^7.2.0"
  }
}
